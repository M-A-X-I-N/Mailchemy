{
  "name": "mailchemy-workspace",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.20.0",
  "engines": {
    "node": "24.21.x",
    "npm": "11.20.x"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
